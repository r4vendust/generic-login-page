import 'package:flutter/material.dart';
import 'package:login_page/screens/register/components/body.dart';

/// A widget to display the content of the registration page
///
/// *[Body] Render the content of the page
class RegisterPage extends StatelessWidget {
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
